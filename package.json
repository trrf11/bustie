{
  "name": "bus80",
  "version": "1.1.5",
  "private": true,
  "description": "Real-time Bus 80 tracker (Zandvoort - Amsterdam)",
  "scripts": {
    "version:patch": "npm version patch -m 'v%s' && git push --follow-tags origin main",
    "version:minor": "npm version minor -m 'v%s' && git push --follow-tags origin main",
    "version:major": "npm version major -m 'v%s' && git push --follow-tags origin main"
  }
}
