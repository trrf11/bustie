# Umami analytics
UMAMI_DB_PASSWORD=
UMAMI_APP_SECRET=

# App version (injected into frontend at build time)
# Set automatically by deploy script, or manually: APP_VERSION=$(node -p "require('./package.json').version")
APP_VERSION=
